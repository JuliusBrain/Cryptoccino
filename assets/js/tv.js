/* Cryptoccino TV — client-side terminal.
 *
 * Built incrementally across PRs. This is the Phase-1 scaffold: it only confirms
 * the page boots and its CSP/asset wiring works. The live-data layer (Binance WS,
 * CoinGecko, alternative.me, DefiLlama) lands in later phases.
 */
(function () {
  "use strict";
  console.info("Cryptoccino TV: scaffold loaded.");
})();
